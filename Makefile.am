SUBDIRS = src doc
include aminclude.am
EXTRA_DIST = Doxyfile trees/*tax* old_Makefile bootstrap example/eg* doc/*.pdf doc/*.tex doc/manual.bbl doc/manual.blg 
#DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
distdir = $(PACKAGE)-$(VERSION)
